# Agent Sequence Diagrams

This folder contains Mermaid sequence diagrams for comparing agent loop patterns.

- `react-sequence.mmd`: ReAct loop for a simple two-part task.
- `plan-and-execute-sequence.mmd`: single-agent Plan-and-Execute loop with planner, executor, replanner, and finalizer.

The PNG exports are generated from the Mermaid source files.
